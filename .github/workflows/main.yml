name: Semi-Hourly Job Sync
on:
  schedule:
    - cron: '17,47 * * * *' 
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Sync with Upstream
        run: |
          gh repo sync ${{ github.repository }} --branch dev
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
